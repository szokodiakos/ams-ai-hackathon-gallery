## 1. Add auto-focus to GameEmbed

- [x] 1.1 Convert `GameEmbed` to a client component (`"use client"` directive)
- [x] 1.2 Add `useRef` for the iframe element and `useEffect` that focuses the iframe on mount and on the iframe `load` event
- [x] 1.3 Add unit tests for focus behavior

## 2. Verify

- [x] 2.1 Run tests and build to ensure nothing is broken
